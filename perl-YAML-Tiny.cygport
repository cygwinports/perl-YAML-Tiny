CPAN_AUTHOR=adamk
inherit perl

NAME="perl-YAML-Tiny"
VERSION=1.51
RELEASE=1
CATEGORY="Perl"
SUMMARY="Perl YAML subset parser module"
DESCRIPTION="YAML::Tiny can parse a limited subset of YAML. But the subset
supported contains most of the features for the more common usese of YAML,
without the memory requirements of the full YAML parser."
